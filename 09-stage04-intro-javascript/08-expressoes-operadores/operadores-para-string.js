// Operador de String (String Operator)

// comparison
// 'a == 'a'

// concatenação
// Retorna a união das strings
let alpha = 'alpha'
console.log(alpha + 'bet' + 's')
// alphabets

// outra forma
let alpha2 = 'alpha'
alpha2 += 'bets'
console.log(alpha2)